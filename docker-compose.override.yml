services:
  api:
    ports:
      - "3001:3001"
    volumes:
      - ./backend/.env:/app/.env

  frontend:
    ports:
      - "3000:3000"
    command: ["sh", "-c", "if [ -d /app/node_modules ]; then npm run dev; else npm install && npm run dev; fi"]

  postgres:
    ports:
      - "5432:5432"
