FROM node:18-alpine AS deps
WORKDIR /app
COPY package*.json ./
RUN npm ci

FROM node:18-alpine
WORKDIR /app
RUN apk add --no-cache curl bash && mkdir -p /app/uploads/tickets && chown -R node:node /app/uploads
COPY --from=deps /app/node_modules ./node_modules
COPY . .
USER node
EXPOSE 3001
